{
  "name": "library-sample",
  "private": true,
  "version": "0.0.0",
  "description": "A library application for AngularJS",
  "repository": "https://github.com/Jorge-Aguirre/library-sample",
  "devDependencies": {
    "bower": "^1.7.7"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server ./app -a localhost -p 8000 -c-1"
  }
}